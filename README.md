To configure the spring boot application for development and production using a jar file in eclipse

1, first create two properties applications-prod and applicartion-dev.
2. In the run configurations in elipse depending on the profile you want, execute --spring.profiles.active=prod or --spring.profiles.active=dev


